apiVersion: v1
kind: Service
metadata:
  name: agent
  labels:
    app: agent
spec:
  type: LoadBalancer
  selector:
    app: agent
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
