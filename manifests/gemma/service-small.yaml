apiVersion: v1
kind: Service
metadata:
  name: gemma-small
  labels:
    app: gemma-small
spec:
  type: ClusterIP
  selector:
    app: gemma-small
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
