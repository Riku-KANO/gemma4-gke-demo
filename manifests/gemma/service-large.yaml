apiVersion: v1
kind: Service
metadata:
  name: gemma-large
  labels:
    app: gemma-large
spec:
  type: ClusterIP
  selector:
    app: gemma-large
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
